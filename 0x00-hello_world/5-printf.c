#include <stdio.h>
/**
 * main - serves as the entry point of the program
 *
 * Return:by default returns zero if no errors returns non-zero value
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
